# Tax Document Checklist Generator

This project scans historical tax documents and generates a deduplicated checklist of everything needed for an upcoming tax filing.

## Usage

Run `/tax-checklist` to generate a checklist. The skill will scan the current working directory for tax documents, render and visually examine every page of every PDF, and produce a categorized Markdown checklist.

## Prerequisites

All dependencies are auto-installed as needed.
